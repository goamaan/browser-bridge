# @goamaan/browser-bridge-sdk

JS and TypeScript SDK for `browser-bridge`.

Install with `bun add @goamaan/browser-bridge-sdk` or `npm i @goamaan/browser-bridge-sdk`.

See the root project README for setup, live-browser attach, and agent workflow guidance.
